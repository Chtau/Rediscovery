syntax = "proto3";

package heartbeat;

service HeartbeatExchange {
  rpc Beat (stream PingPongMessage) returns (stream PingPongMessage);
}

message PingPongMessage {
  uint64 ticks = 1;
  uint64 lastRoundTripTicks = 2;
  Command command = 3;

  enum Command {
    Beat = 0;
  }
}