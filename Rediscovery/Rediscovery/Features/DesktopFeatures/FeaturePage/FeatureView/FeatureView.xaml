<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ctrl="clr-namespace:Rediscovery.Controls"
             mc:Ignorable="d"
             x:Class="Rediscovery.Features.DesktopFeatures.FeaturePage.FeatureView.FeatureView"
             Title="{Binding Title}"
             Padding="0,0,0,0">
    <ContentPage.ToolbarItems>
        <ToolbarItem x:Name="Setting" Text="Setting" Clicked="Setting_Clicked" Icon="settings_fill.png"/>
        <ToolbarItem x:Name="Back" Text="Back" Clicked="Back_Clicked" Icon="close_white.png"/>
    </ContentPage.ToolbarItems>
    <Grid>
        <ctrl:HybridWebView x:Name="hybridWebView"/>
        <ContentView x:Name="actOverlay" IsVisible="{Binding Load.IsLoading}" 
                         HorizontalOptions="FillAndExpand" 
                         VerticalOptions="FillAndExpand"
                         >
            <StackLayout>
                <ActivityIndicator IsVisible="True" IsRunning="True" x:Name="busyActivity" Style="{DynamicResource ActivityIndicator}"/>
                <Label Style="{DynamicResource ActivityIndicatorLabel}" Text="Loading feature UI" />
            </StackLayout>
        </ContentView>
    </Grid>
</ContentPage>