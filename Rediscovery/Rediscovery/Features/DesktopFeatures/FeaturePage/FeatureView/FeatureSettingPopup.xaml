<?xml version="1.0" encoding="utf-8" ?>
<pages:PopupPage 
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
    xmlns:animations="clr-namespace:Rg.Plugins.Popup.Animations;assembly=Rg.Plugins.Popup"
    xmlns:flex="clr-namespace:Flex.Controls;assembly=Flex"
    x:Class="Rediscovery.Features.DesktopFeatures.FeaturePage.FeatureView.FeatureSettingPopup">
    <pages:PopupPage.Animation>
        <animations:ScaleAnimation 
            PositionIn="Center"
            PositionOut="Center"
            ScaleIn="1.2"
            ScaleOut="0.8"
            DurationIn="400"
            DurationOut="300"
            EasingIn="SinOut"
            EasingOut="SinIn"
            HasBackgroundAnimation="True"/>
    </pages:PopupPage.Animation>
    <StackLayout VerticalOptions="Center" HorizontalOptions="FillAndExpand" Padding="20, 20, 20, 20">
        <StackLayout BackgroundColor="White" Padding="0, 10, 0, 0">
            <Label Text="Settings" TextColor="Gray" FontSize="20" HorizontalOptions="Center"></Label>
            <ScrollView>
                <StackLayout>
                    <Picker x:Name="profilePicker"
                    Title="Select a profile"
                    TitleColor="Blue"
                    ItemsSource="{Binding Profiles}"
                        SelectedItem="{Binding SelectedProfile}"
                        ItemDisplayBinding="{Binding DisplayName}">
                    </Picker>
                    <flex:FlexButton 
                        Icon="close_white.png"
                        Text="Close"
                        Margin="5"
                        Clicked="Close_Clicked"
                    Style="{StaticResource LargeFlexButton}"/>
                </StackLayout>
            </ScrollView>
        </StackLayout>
    </StackLayout>
</pages:PopupPage>