<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:flex="clr-namespace:Flex.Controls;assembly=Flex"
             mc:Ignorable="d"
             x:Class="Rediscovery.Features.DesktopConfiguration.DesktopConfigurationEditPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem IsEnabled="{Binding CanEdit}" Text="Save" Clicked="Save_Clicked" Icon="save_fill.png"/>
        <ToolbarItem IsEnabled="{Binding CanEdit}" Text="Remove" Clicked="Remove_Clicked" Icon="delete_bin_fill.png"/>
        <ToolbarItem Text="Back" Clicked="Back_Clicked" Icon="close_white.png"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <Grid>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <ScrollView Grid.Row="0">
                    <StackLayout Spacing="20" Padding="15">
                        <Label Text="Connection Name" FontSize="Medium" />
                        <Entry Text="{Binding Item.DisplayName}" FontSize="Small" />
                        <Label Text="IP Address" FontSize="Medium" />
                        <Editor Text="{Binding Item.LastKnownAddress}" Keyboard="Url" FontSize="Small" Margin="0" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Label Grid.Column="0" Grid.Row="0" Text="AutoConnect" FontSize="Medium"/>
                            <Switch Grid.Column="1" Grid.Row="0" IsToggled="{Binding Item.AutoConnect}" />
                            <Label Grid.Column="0" Grid.Row="1" Text="ConnectionState:" FontSize="Medium" />
                            <Label Grid.Column="1" Grid.Row="1" Text="{Binding Item.ConnectionState, Converter={StaticResource connectionStateConverter}}" FontSize="Medium"/>
                            <Label Grid.Column="0" Grid.Row="2" Text="LastConnection:" FontSize="Medium" />
                            <Label Grid.Column="1" Grid.Row="2" Text="{Binding Item.LastConnection}" FontSize="Medium"/>
                        </Grid>
                    </StackLayout>
                </ScrollView>
                <StackLayout Grid.Row="1">
                    <flex:FlexButton 
                        Icon="remote_control_fill.png"
                    Text="Connect"
                    Margin="5"
                    IsVisible="{Binding Item.ConnectionState, Converter={StaticResource connectionStateReverseBoolConverter}}"
                    ClickedCommand="{Binding Connect}"
                    Style="{StaticResource LargeFlexButton}"/>
                    <flex:FlexButton 
                        Icon="wifi_off.png"
                    Text="Disconnect"
                    Margin="5"
                    IsVisible="{Binding Item.ConnectionState, Converter={StaticResource connectionStateBoolConverter}}"
                    ClickedCommand="{Binding Disconnect}"
                    Style="{StaticResource LargeFlexButton}"/>
                </StackLayout>
            </Grid>
            <ContentView x:Name="actOverlay" IsVisible="{Binding Load.IsLoading}" 
                         HorizontalOptions="FillAndExpand" 
                         VerticalOptions="FillAndExpand">
                <AbsoluteLayout>
                    <StackLayout AbsoluteLayout.LayoutFlags="All"
                                 Style="{DynamicResource ActivityBackgroundOverlay}"
                                 AbsoluteLayout.LayoutBounds="1,1,1,1"></StackLayout>
                    <StackLayout AbsoluteLayout.LayoutFlags="All" AbsoluteLayout.LayoutBounds="1,1,1,1"
                                 HorizontalOptions="FillAndExpand" 
                         VerticalOptions="FillAndExpand">
                        <ActivityIndicator IsVisible="True" IsRunning="True" x:Name="busyActivity" Style="{DynamicResource ActivityIndicator}"/>
                        <Label Style="{DynamicResource ActivityIndicatorLabel}" Text="Connecting to Desktop" />
                    </StackLayout>
                </AbsoluteLayout>
            </ContentView>
        </Grid>
    </ContentPage.Content>
</ContentPage>