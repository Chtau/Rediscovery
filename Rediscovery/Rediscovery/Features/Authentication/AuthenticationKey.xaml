<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Rediscovery.Features.Authentication.AuthenticationKey"
             Title="Authentication Key">
    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="Primary">#2196F3</Color>
            <Color x:Key="Accent">#96d1ff</Color>
            <Color x:Key="LightTextColor">#999999</Color>
            <Color x:Key="Dismiss">#840000</Color>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Close" Clicked="Close_Clicked" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <Grid>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <StackLayout Grid.Row="0" Orientation="Vertical" Padding="16,40,16,40" Spacing="10">
                    <Label FontSize="22" HorizontalOptions="Center">
                        <Label.FormattedText>
                            <FormattedString>
                                <FormattedString.Spans>
                                    <Span Text="Authentication Key" FontAttributes="Bold" FontSize="22" />
                                </FormattedString.Spans>
                            </FormattedString>
                        </Label.FormattedText>
                    </Label>
                    <Label>
                        <Label.FormattedText>
                            <FormattedString>
                                <FormattedString.Spans>
                                    <Span Text="Enter the Authentication Key from the" />
                                    <Span Text=" " />
                                    <Span Text="Rediscovery Desktop" FontAttributes="Bold" />
                                    <Span Text=" Application." />
                                </FormattedString.Spans>
                            </FormattedString>
                        </Label.FormattedText>
                    </Label>
                    <StackLayout Padding="25" HorizontalOptions="Center" WidthRequest="200">
                        <Label Text="Key" FontSize="Medium" HorizontalOptions="Center" />
                        <Editor Text="{Binding Item.Key}" FontSize="Medium"/>
                    </StackLayout>
                    <Button Margin="0,10,0,0" Text="Verify"
                        Command="{Binding KeyVerify}"
                        BackgroundColor="{StaticResource Primary}"
                        TextColor="White" />
                    <Label HorizontalOptions="Center" Margin="0, 25, 0, 0">
                        <Label.FormattedText>
                            <FormattedString>
                                <FormattedString.Spans>
                                    <Span Text="Server response:" />
                                    <Span Text=" " />
                                    <Span FontSize="16" TextColor="{StaticResource Dismiss}" Text="Denied" FontAttributes="Bold" />
                                </FormattedString.Spans>
                            </FormattedString>
                        </Label.FormattedText>
                    </Label>
                </StackLayout>
                <StackLayout Grid.Row="1">
                    <Button Text="Dismiss" Clicked="Close_Clicked"
                            BackgroundColor="{StaticResource Dismiss}"
                        TextColor="White"/>
                </StackLayout>
            </Grid>
        </Grid>
    </ContentPage.Content>
</ContentPage>