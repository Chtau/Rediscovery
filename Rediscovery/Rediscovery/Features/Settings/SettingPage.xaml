<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Rediscovery.Features.Settings.SettingPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Save" Command="{Binding SaveCommand}" Icon="save_fill_green.png"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <ScrollView Grid.Row="0">
                <StackLayout Spacing="20" Padding="15">
                    <StackLayout>
                        <Label Text="Device Name" FontSize="Medium" />
                        <Entry Text="{Binding Setting.DeviceIdentifier}" FontSize="Small" />
                    </StackLayout>
                    <StackLayout>
                        <Label Text="Discovery Port" FontSize="Medium" />
                        <Entry Text="{Binding Setting.DiscoveryPort}" Keyboard="Numeric" FontSize="Small" />
                    </StackLayout>
                </StackLayout>
            </ScrollView>
            <StackLayout Grid.Row="1" Margin="10">
                <Label Text="Rediscovery Mobile Client by Christoph Taucher"/>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>